name: test-http-autonaming
runtime:
  name: yaml
resources:
  testedResource:
    type: gcp:compute:HttpHealthCheck
    properties:
      checkIntervalSec: 1
      requestPath: /health_check
      timeoutSec: 1
