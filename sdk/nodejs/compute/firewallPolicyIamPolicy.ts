// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Three different resources help you manage your IAM policy for Compute Engine FirewallPolicy. Each of these resources serves a different use case:
 *
 * * `gcp.compute.FirewallPolicyIamPolicy`: Authoritative. Sets the IAM policy for the firewallpolicy and replaces any existing policy already attached.
 * * `gcp.compute.FirewallPolicyIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the firewallpolicy are preserved.
 * * `gcp.compute.FirewallPolicyIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the firewallpolicy are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.compute.FirewallPolicyIamPolicy`: Retrieves the IAM policy for the firewallpolicy
 *
 * > **Note:** `gcp.compute.FirewallPolicyIamPolicy` **cannot** be used in conjunction with `gcp.compute.FirewallPolicyIamBinding` and `gcp.compute.FirewallPolicyIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.compute.FirewallPolicyIamBinding` resources **can be** used in conjunction with `gcp.compute.FirewallPolicyIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.compute.FirewallPolicyIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/viewer",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.compute.FirewallPolicyIamPolicy("policy", {
 *     name: _default.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.compute.FirewallPolicyIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.compute.FirewallPolicyIamBinding("binding", {
 *     name: _default.name,
 *     role: "roles/viewer",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.compute.FirewallPolicyIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.compute.FirewallPolicyIamMember("member", {
 *     name: _default.name,
 *     role: "roles/viewer",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *
 * full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 * ---
 *
 * # IAM policy for Compute Engine FirewallPolicy
 *
 * Three different resources help you manage your IAM policy for Compute Engine FirewallPolicy. Each of these resources serves a different use case:
 *
 * * `gcp.compute.FirewallPolicyIamPolicy`: Authoritative. Sets the IAM policy for the firewallpolicy and replaces any existing policy already attached.
 * * `gcp.compute.FirewallPolicyIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the firewallpolicy are preserved.
 * * `gcp.compute.FirewallPolicyIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the firewallpolicy are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.compute.FirewallPolicyIamPolicy`: Retrieves the IAM policy for the firewallpolicy
 *
 * > **Note:** `gcp.compute.FirewallPolicyIamPolicy` **cannot** be used in conjunction with `gcp.compute.FirewallPolicyIamBinding` and `gcp.compute.FirewallPolicyIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.compute.FirewallPolicyIamBinding` resources **can be** used in conjunction with `gcp.compute.FirewallPolicyIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.compute.FirewallPolicyIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/viewer",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.compute.FirewallPolicyIamPolicy("policy", {
 *     name: _default.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.compute.FirewallPolicyIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.compute.FirewallPolicyIamBinding("binding", {
 *     name: _default.name,
 *     role: "roles/viewer",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.compute.FirewallPolicyIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.compute.FirewallPolicyIamMember("member", {
 *     name: _default.name,
 *     role: "roles/viewer",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## Import
 *
 * For all import syntaxes, the "resource in question" can take any of the following forms:
 *
 * * locations/global/firewallPolicies/{{name}}
 * * {{name}}
 *
 * Any variables not passed in the import command will be taken from the provider configuration.
 *
 * Compute Engine firewallpolicy IAM resources can be imported using the resource identifiers, role, and member.
 *
 * IAM member imports use space-delimited identifiers: the resource in question, the role, and the member identity, e.g.
 * ```sh
 * $ terraform import google_compute_firewall_policy_iam_member.editor "locations/global/firewallPolicies/{{firewall_policy}} roles/viewer user:jane@example.com"
 * ```
 *
 * IAM binding imports use space-delimited identifiers: the resource in question and the role, e.g.
 * ```sh
 * $ terraform import google_compute_firewall_policy_iam_binding.editor "locations/global/firewallPolicies/{{firewall_policy}} roles/viewer"
 * ```
 *
 * IAM policy imports use the identifier of the resource in question, e.g.
 * ```sh
 * $ pulumi import gcp:compute/firewallPolicyIamPolicy:FirewallPolicyIamPolicy editor locations/global/firewallPolicies/{{firewall_policy}}
 * ```
 *
 * > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *  full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 */
export class FirewallPolicyIamPolicy extends pulumi.CustomResource {
    /**
     * Get an existing FirewallPolicyIamPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FirewallPolicyIamPolicyState, opts?: pulumi.CustomResourceOptions): FirewallPolicyIamPolicy {
        return new FirewallPolicyIamPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:compute/firewallPolicyIamPolicy:FirewallPolicyIamPolicy';

    /**
     * Returns true if the given object is an instance of FirewallPolicyIamPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FirewallPolicyIamPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FirewallPolicyIamPolicy.__pulumiType;
    }

    /**
     * (Computed) The etag of the IAM policy.
     */
    declare public /*out*/ readonly etag: pulumi.Output<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    declare public readonly policyData: pulumi.Output<string>;

    /**
     * Create a FirewallPolicyIamPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FirewallPolicyIamPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FirewallPolicyIamPolicyArgs | FirewallPolicyIamPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FirewallPolicyIamPolicyState | undefined;
            resourceInputs["etag"] = state?.etag;
            resourceInputs["name"] = state?.name;
            resourceInputs["policyData"] = state?.policyData;
        } else {
            const args = argsOrState as FirewallPolicyIamPolicyArgs | undefined;
            if (args?.policyData === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyData'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["policyData"] = args?.policyData;
            resourceInputs["etag"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FirewallPolicyIamPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FirewallPolicyIamPolicy resources.
 */
export interface FirewallPolicyIamPolicyState {
    /**
     * (Computed) The etag of the IAM policy.
     */
    etag?: pulumi.Input<string | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a FirewallPolicyIamPolicy resource.
 */
export interface FirewallPolicyIamPolicyArgs {
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData: pulumi.Input<string>;
}
