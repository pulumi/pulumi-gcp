// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * ## Example Usage
 *
 * ### Compute Region Health Source Basic
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const hap = new gcp.compute.RegionHealthAggregationPolicy("hap", {
 *     name: "test-health-source-hap",
 *     description: "health aggregation policy for health source",
 *     region: "us-central1",
 * });
 * const _default = new gcp.compute.HealthCheck("default", {
 *     name: "test-health-source-hc",
 *     httpHealthCheck: {
 *         port: 80,
 *     },
 * });
 * const defaultRegionBackendService = new gcp.compute.RegionBackendService("default", {
 *     name: "test-health-source-bs",
 *     region: "us-central1",
 *     healthChecks: _default.id,
 *     loadBalancingScheme: "INTERNAL",
 * });
 * const exampleTestHealthSource = new gcp.compute.RegionHealthSource("example_test_health_source", {
 *     name: "test-health-source",
 *     description: "Example health source basic",
 *     region: "us-central1",
 *     sourceType: "BACKEND_SERVICE",
 *     sources: defaultRegionBackendService.id,
 *     healthAggregationPolicy: hap.id,
 * });
 * ```
 *
 * ## Import
 *
 * RegionHealthSource can be imported using any of these accepted formats:
 *
 * * `projects/{{project}}/regions/{{region}}/healthSources/{{name}}`
 *
 * * `{{project}}/{{region}}/{{name}}`
 *
 * * `{{region}}/{{name}}`
 *
 * * `{{name}}`
 *
 * When using the `pulumi import` command, RegionHealthSource can be imported using one of the formats above. For example:
 *
 * ```sh
 * $ pulumi import gcp:compute/regionHealthSource:RegionHealthSource default projects/{{project}}/regions/{{region}}/healthSources/{{name}}
 * ```
 *
 * ```sh
 * $ pulumi import gcp:compute/regionHealthSource:RegionHealthSource default {{project}}/{{region}}/{{name}}
 * ```
 *
 * ```sh
 * $ pulumi import gcp:compute/regionHealthSource:RegionHealthSource default {{region}}/{{name}}
 * ```
 *
 * ```sh
 * $ pulumi import gcp:compute/regionHealthSource:RegionHealthSource default {{name}}
 * ```
 */
export class RegionHealthSource extends pulumi.CustomResource {
    /**
     * Get an existing RegionHealthSource resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RegionHealthSourceState, opts?: pulumi.CustomResourceOptions): RegionHealthSource {
        return new RegionHealthSource(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:compute/regionHealthSource:RegionHealthSource';

    /**
     * Returns true if the given object is an instance of RegionHealthSource.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RegionHealthSource {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RegionHealthSource.__pulumiType;
    }

    /**
     * Creation timestamp in RFC3339 text format.
     */
    declare public /*out*/ readonly creationTimestamp: pulumi.Output<string>;
    /**
     * An optional description of this resource. Provide this property when you
     * create the resource.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * Fingerprint of this resource. A hash of the contents stored in this object.
     * This field is used in optimistic locking.
     */
    declare public /*out*/ readonly fingerprint: pulumi.Output<string>;
    /**
     * URL to the `HealthAggregationPolicy` resource. Must be set. Must
     * be regional and in the same region as the `HealthSource`. Can be
     * mutated.
     */
    declare public readonly healthAggregationPolicy: pulumi.Output<string | undefined>;
    /**
     * Name of the resource. Provided by the client when the resource is created.
     * The name must be 1-63 characters long, and comply with RFC1035.
     * Specifically, the name must be 1-63 characters long and match the regular
     * expression `a-z?` which means the first
     * character must be a lowercase letter, and all following characters must
     * be a dash, lowercase letter, or digit, except the last character, which
     * cannot be a dash.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;
    /**
     * URL of the region where the health source resides.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * Server-defined URL with id for the resource.
     */
    declare public /*out*/ readonly selfLinkWithId: pulumi.Output<string>;
    /**
     * Specifies the type of the `HealthSource`. The only allowed value
     * is `BACKEND_SERVICE`. Must be specified when the
     * `HealthSource` is created, and cannot be mutated.
     * Possible values are: `BACKEND_SERVICE`.
     */
    declare public readonly sourceType: pulumi.Output<string>;
    /**
     * URLs to the source resources. Must be size 1. Must be a
     * `BackendService` if the `sourceType` is `BACKEND_SERVICE`. The
     * `BackendService` must have load balancing scheme
     * `INTERNAL` or `INTERNAL_MANAGED` and must be regional
     * and in the same region as the `HealthSource` (cross-region
     * deployment for `INTERNAL_MANAGED` is not supported). The
     * `BackendService` may use only IGs, MIGs, or NEGs of type
     * `GCE_VM_IP` or `GCE_VM_IP_PORT`. The
     * `BackendService` may not use `haPolicy`. Can be
     * mutated.
     */
    declare public readonly sources: pulumi.Output<string | undefined>;

    /**
     * Create a RegionHealthSource resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RegionHealthSourceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RegionHealthSourceArgs | RegionHealthSourceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RegionHealthSourceState | undefined;
            resourceInputs["creationTimestamp"] = state?.creationTimestamp;
            resourceInputs["description"] = state?.description;
            resourceInputs["fingerprint"] = state?.fingerprint;
            resourceInputs["healthAggregationPolicy"] = state?.healthAggregationPolicy;
            resourceInputs["name"] = state?.name;
            resourceInputs["project"] = state?.project;
            resourceInputs["region"] = state?.region;
            resourceInputs["selfLinkWithId"] = state?.selfLinkWithId;
            resourceInputs["sourceType"] = state?.sourceType;
            resourceInputs["sources"] = state?.sources;
        } else {
            const args = argsOrState as RegionHealthSourceArgs | undefined;
            if (args?.region === undefined && !opts.urn) {
                throw new Error("Missing required property 'region'");
            }
            if (args?.sourceType === undefined && !opts.urn) {
                throw new Error("Missing required property 'sourceType'");
            }
            resourceInputs["description"] = args?.description;
            resourceInputs["healthAggregationPolicy"] = args?.healthAggregationPolicy;
            resourceInputs["name"] = args?.name;
            resourceInputs["project"] = args?.project;
            resourceInputs["region"] = args?.region;
            resourceInputs["sourceType"] = args?.sourceType;
            resourceInputs["sources"] = args?.sources;
            resourceInputs["creationTimestamp"] = undefined /*out*/;
            resourceInputs["fingerprint"] = undefined /*out*/;
            resourceInputs["selfLinkWithId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RegionHealthSource.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RegionHealthSource resources.
 */
export interface RegionHealthSourceState {
    /**
     * Creation timestamp in RFC3339 text format.
     */
    creationTimestamp?: pulumi.Input<string>;
    /**
     * An optional description of this resource. Provide this property when you
     * create the resource.
     */
    description?: pulumi.Input<string>;
    /**
     * Fingerprint of this resource. A hash of the contents stored in this object.
     * This field is used in optimistic locking.
     */
    fingerprint?: pulumi.Input<string>;
    /**
     * URL to the `HealthAggregationPolicy` resource. Must be set. Must
     * be regional and in the same region as the `HealthSource`. Can be
     * mutated.
     */
    healthAggregationPolicy?: pulumi.Input<string>;
    /**
     * Name of the resource. Provided by the client when the resource is created.
     * The name must be 1-63 characters long, and comply with RFC1035.
     * Specifically, the name must be 1-63 characters long and match the regular
     * expression `a-z?` which means the first
     * character must be a lowercase letter, and all following characters must
     * be a dash, lowercase letter, or digit, except the last character, which
     * cannot be a dash.
     */
    name?: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    project?: pulumi.Input<string>;
    /**
     * URL of the region where the health source resides.
     */
    region?: pulumi.Input<string>;
    /**
     * Server-defined URL with id for the resource.
     */
    selfLinkWithId?: pulumi.Input<string>;
    /**
     * Specifies the type of the `HealthSource`. The only allowed value
     * is `BACKEND_SERVICE`. Must be specified when the
     * `HealthSource` is created, and cannot be mutated.
     * Possible values are: `BACKEND_SERVICE`.
     */
    sourceType?: pulumi.Input<string>;
    /**
     * URLs to the source resources. Must be size 1. Must be a
     * `BackendService` if the `sourceType` is `BACKEND_SERVICE`. The
     * `BackendService` must have load balancing scheme
     * `INTERNAL` or `INTERNAL_MANAGED` and must be regional
     * and in the same region as the `HealthSource` (cross-region
     * deployment for `INTERNAL_MANAGED` is not supported). The
     * `BackendService` may use only IGs, MIGs, or NEGs of type
     * `GCE_VM_IP` or `GCE_VM_IP_PORT`. The
     * `BackendService` may not use `haPolicy`. Can be
     * mutated.
     */
    sources?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a RegionHealthSource resource.
 */
export interface RegionHealthSourceArgs {
    /**
     * An optional description of this resource. Provide this property when you
     * create the resource.
     */
    description?: pulumi.Input<string>;
    /**
     * URL to the `HealthAggregationPolicy` resource. Must be set. Must
     * be regional and in the same region as the `HealthSource`. Can be
     * mutated.
     */
    healthAggregationPolicy?: pulumi.Input<string>;
    /**
     * Name of the resource. Provided by the client when the resource is created.
     * The name must be 1-63 characters long, and comply with RFC1035.
     * Specifically, the name must be 1-63 characters long and match the regular
     * expression `a-z?` which means the first
     * character must be a lowercase letter, and all following characters must
     * be a dash, lowercase letter, or digit, except the last character, which
     * cannot be a dash.
     */
    name?: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    project?: pulumi.Input<string>;
    /**
     * URL of the region where the health source resides.
     */
    region: pulumi.Input<string>;
    /**
     * Specifies the type of the `HealthSource`. The only allowed value
     * is `BACKEND_SERVICE`. Must be specified when the
     * `HealthSource` is created, and cannot be mutated.
     * Possible values are: `BACKEND_SERVICE`.
     */
    sourceType: pulumi.Input<string>;
    /**
     * URLs to the source resources. Must be size 1. Must be a
     * `BackendService` if the `sourceType` is `BACKEND_SERVICE`. The
     * `BackendService` must have load balancing scheme
     * `INTERNAL` or `INTERNAL_MANAGED` and must be regional
     * and in the same region as the `HealthSource` (cross-region
     * deployment for `INTERNAL_MANAGED` is not supported). The
     * `BackendService` may use only IGs, MIGs, or NEGs of type
     * `GCE_VM_IP` or `GCE_VM_IP_PORT`. The
     * `BackendService` may not use `haPolicy`. Can be
     * mutated.
     */
    sources?: pulumi.Input<string>;
}
