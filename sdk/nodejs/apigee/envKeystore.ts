// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * An `Environment KeyStore` in Apigee.
 *
 * To get more information about EnvKeystore, see:
 *
 * * [API documentation](https://cloud.google.com/apigee/docs/reference/apis/apigee/rest/v1/organizations.environments.keystores/create)
 * * How-to Guides
 *     * [Creating an environment](https://cloud.google.com/apigee/docs/api-platform/get-started/create-environment)
 *
 * ## Import
 *
 * EnvKeystore can be imported using any of these accepted formats:
 *
 * * `{{env_id}}/keystores/{{name}}`
 *
 * * `{{env_id}}/{{name}}`
 *
 * When using the `pulumi import` command, EnvKeystore can be imported using one of the formats above. For example:
 *
 * ```sh
 * $ pulumi import gcp:apigee/envKeystore:EnvKeystore default {{env_id}}/keystores/{{name}}
 * ```
 *
 * ```sh
 * $ pulumi import gcp:apigee/envKeystore:EnvKeystore default {{env_id}}/{{name}}
 * ```
 */
export class EnvKeystore extends pulumi.CustomResource {
    /**
     * Get an existing EnvKeystore resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: EnvKeystoreState, opts?: pulumi.CustomResourceOptions): EnvKeystore {
        return new EnvKeystore(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:apigee/envKeystore:EnvKeystore';

    /**
     * Returns true if the given object is an instance of EnvKeystore.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is EnvKeystore {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EnvKeystore.__pulumiType;
    }

    /**
     * Aliases in this keystore.
     */
    public /*out*/ readonly aliases!: pulumi.Output<string[]>;
    /**
     * The Apigee environment group associated with the Apigee environment,
     * in the format `organizations/{{org_name}}/environments/{{env_name}}`.
     *
     *
     * - - -
     */
    public readonly envId!: pulumi.Output<string>;
    /**
     * The name of the newly created keystore.
     */
    public readonly name!: pulumi.Output<string>;

    /**
     * Create a EnvKeystore resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: EnvKeystoreArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: EnvKeystoreArgs | EnvKeystoreState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as EnvKeystoreState | undefined;
            resourceInputs["aliases"] = state ? state.aliases : undefined;
            resourceInputs["envId"] = state ? state.envId : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
        } else {
            const args = argsOrState as EnvKeystoreArgs | undefined;
            if ((!args || args.envId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'envId'");
            }
            resourceInputs["envId"] = args ? args.envId : undefined;
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["aliases"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EnvKeystore.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering EnvKeystore resources.
 */
export interface EnvKeystoreState {
    /**
     * Aliases in this keystore.
     */
    aliases?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The Apigee environment group associated with the Apigee environment,
     * in the format `organizations/{{org_name}}/environments/{{env_name}}`.
     *
     *
     * - - -
     */
    envId?: pulumi.Input<string>;
    /**
     * The name of the newly created keystore.
     */
    name?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a EnvKeystore resource.
 */
export interface EnvKeystoreArgs {
    /**
     * The Apigee environment group associated with the Apigee environment,
     * in the format `organizations/{{org_name}}/environments/{{env_name}}`.
     *
     *
     * - - -
     */
    envId: pulumi.Input<string>;
    /**
     * The name of the newly created keystore.
     */
    name?: pulumi.Input<string>;
}
