// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Three different resources help you manage your IAM policy for BigLake Hive Metastore HiveDatabase. Each of these resources serves a different use case:
 *
 * * `gcp.biglake.HiveDatabaseIamPolicy`: Authoritative. Sets the IAM policy for the hivedatabase and replaces any existing policy already attached.
 * * `gcp.biglake.HiveDatabaseIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the hivedatabase are preserved.
 * * `gcp.biglake.HiveDatabaseIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the hivedatabase are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.biglake.HiveDatabaseIamPolicy`: Retrieves the IAM policy for the hivedatabase
 *
 * > **Note:** `gcp.biglake.HiveDatabaseIamPolicy` **cannot** be used in conjunction with `gcp.biglake.HiveDatabaseIamBinding` and `gcp.biglake.HiveDatabaseIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.biglake.HiveDatabaseIamBinding` resources **can be** used in conjunction with `gcp.biglake.HiveDatabaseIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * > **Warning:** This resource is in beta, and should be used with the terraform-provider-google-beta provider.
 * See Provider Versions for more details on beta resources.
 *
 * ## gcp.biglake.HiveDatabaseIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/biglake.editor",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.biglake.HiveDatabaseIamPolicy("policy", {
 *     project: myHiveDatabase.project,
 *     catalog: myHiveDatabase.catalog,
 *     name: myHiveDatabase.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.biglake.HiveDatabaseIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.biglake.HiveDatabaseIamBinding("binding", {
 *     project: myHiveDatabase.project,
 *     catalog: myHiveDatabase.catalog,
 *     name: myHiveDatabase.name,
 *     role: "roles/biglake.editor",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.biglake.HiveDatabaseIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.biglake.HiveDatabaseIamMember("member", {
 *     project: myHiveDatabase.project,
 *     catalog: myHiveDatabase.catalog,
 *     name: myHiveDatabase.name,
 *     role: "roles/biglake.editor",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## This resource supports User Project Overrides.
 *
 * - 
 *
 * # IAM policy for BigLake Hive Metastore HiveDatabase
 *
 * Three different resources help you manage your IAM policy for BigLake Hive Metastore HiveDatabase. Each of these resources serves a different use case:
 *
 * * `gcp.biglake.HiveDatabaseIamPolicy`: Authoritative. Sets the IAM policy for the hivedatabase and replaces any existing policy already attached.
 * * `gcp.biglake.HiveDatabaseIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the hivedatabase are preserved.
 * * `gcp.biglake.HiveDatabaseIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the hivedatabase are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.biglake.HiveDatabaseIamPolicy`: Retrieves the IAM policy for the hivedatabase
 *
 * > **Note:** `gcp.biglake.HiveDatabaseIamPolicy` **cannot** be used in conjunction with `gcp.biglake.HiveDatabaseIamBinding` and `gcp.biglake.HiveDatabaseIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.biglake.HiveDatabaseIamBinding` resources **can be** used in conjunction with `gcp.biglake.HiveDatabaseIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * > **Warning:** This resource is in beta, and should be used with the terraform-provider-google-beta provider.
 * See Provider Versions for more details on beta resources.
 *
 * ## gcp.biglake.HiveDatabaseIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/biglake.editor",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.biglake.HiveDatabaseIamPolicy("policy", {
 *     project: myHiveDatabase.project,
 *     catalog: myHiveDatabase.catalog,
 *     name: myHiveDatabase.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.biglake.HiveDatabaseIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.biglake.HiveDatabaseIamBinding("binding", {
 *     project: myHiveDatabase.project,
 *     catalog: myHiveDatabase.catalog,
 *     name: myHiveDatabase.name,
 *     role: "roles/biglake.editor",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.biglake.HiveDatabaseIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.biglake.HiveDatabaseIamMember("member", {
 *     project: myHiveDatabase.project,
 *     catalog: myHiveDatabase.catalog,
 *     name: myHiveDatabase.name,
 *     role: "roles/biglake.editor",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## Import
 *
 * For all import syntaxes, the "resource in question" can take any of the following forms:
 *
 * * projects/{{project}}/catalogs/{{catalog}}/namespaces/{{name}}
 * * {{project}}/{{catalog}}/{{name}}
 * * {{catalog}}/{{name}}
 * * {{name}}
 *
 * Any variables not passed in the import command will be taken from the provider configuration.
 *
 * BigLake Hive Metastore hivedatabase IAM resources can be imported using the resource identifiers, role, and member.
 *
 * IAM member imports use space-delimited identifiers: the resource in question, the role, and the member identity, e.g.
 * ```sh
 * $ terraform import google_biglake_hive_database_iam_member.editor "projects/{{project}}/catalogs/{{catalog}}/namespaces/{{hive_database}} roles/biglake.editor user:jane@example.com"
 * ```
 *
 * IAM binding imports use space-delimited identifiers: the resource in question and the role, e.g.
 * ```sh
 * $ terraform import google_biglake_hive_database_iam_binding.editor "projects/{{project}}/catalogs/{{catalog}}/namespaces/{{hive_database}} roles/biglake.editor"
 * ```
 *
 * IAM policy imports use the identifier of the resource in question, e.g.
 * ```sh
 * $ pulumi import gcp:biglake/hiveDatabaseIamPolicy:HiveDatabaseIamPolicy editor projects/{{project}}/catalogs/{{catalog}}/namespaces/{{hive_database}}
 * ```
 *
 * > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *  full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 */
export class HiveDatabaseIamPolicy extends pulumi.CustomResource {
    /**
     * Get an existing HiveDatabaseIamPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: HiveDatabaseIamPolicyState, opts?: pulumi.CustomResourceOptions): HiveDatabaseIamPolicy {
        return new HiveDatabaseIamPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:biglake/hiveDatabaseIamPolicy:HiveDatabaseIamPolicy';

    /**
     * Returns true if the given object is an instance of HiveDatabaseIamPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is HiveDatabaseIamPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === HiveDatabaseIamPolicy.__pulumiType;
    }

    /**
     * Hive catalog where the database to create is located.
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly catalog: pulumi.Output<string>;
    /**
     * (Computed) The etag of the IAM policy.
     */
    declare public /*out*/ readonly etag: pulumi.Output<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    declare public readonly policyData: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;

    /**
     * Create a HiveDatabaseIamPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: HiveDatabaseIamPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: HiveDatabaseIamPolicyArgs | HiveDatabaseIamPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as HiveDatabaseIamPolicyState | undefined;
            resourceInputs["catalog"] = state?.catalog;
            resourceInputs["etag"] = state?.etag;
            resourceInputs["name"] = state?.name;
            resourceInputs["policyData"] = state?.policyData;
            resourceInputs["project"] = state?.project;
        } else {
            const args = argsOrState as HiveDatabaseIamPolicyArgs | undefined;
            if (args?.catalog === undefined && !opts.urn) {
                throw new Error("Missing required property 'catalog'");
            }
            if (args?.policyData === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyData'");
            }
            resourceInputs["catalog"] = args?.catalog;
            resourceInputs["name"] = args?.name;
            resourceInputs["policyData"] = args?.policyData;
            resourceInputs["project"] = args?.project;
            resourceInputs["etag"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(HiveDatabaseIamPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering HiveDatabaseIamPolicy resources.
 */
export interface HiveDatabaseIamPolicyState {
    /**
     * Hive catalog where the database to create is located.
     * Used to find the parent resource to bind the IAM policy to
     */
    catalog?: pulumi.Input<string | undefined>;
    /**
     * (Computed) The etag of the IAM policy.
     */
    etag?: pulumi.Input<string | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a HiveDatabaseIamPolicy resource.
 */
export interface HiveDatabaseIamPolicyArgs {
    /**
     * Hive catalog where the database to create is located.
     * Used to find the parent resource to bind the IAM policy to
     */
    catalog: pulumi.Input<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
}
