// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * A Snooze will prevent any alerts from being opened, and close any that are already open.
 * The Snooze will work on alerts that match the criteria defined in the Snooze.
 * The Snooze will be active from interval.start_time through interval.end_time.
 *
 * > **Note:** Monitoring Snoozes cannot be deleted from the Google Cloud Platform.
 * Destroying a Terraform-managed Snooze will cancel the snooze and remove it from state but
 * *will not delete the resource from the project.*
 *
 * To get more information about Snooze, see:
 *
 * * [API documentation](https://cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.snoozes)
 * * How-to Guides
 *     * [Official Documentation](https://cloud.google.com/monitoring/alerts/snooze)
 *
 * ## Example Usage
 *
 * ### Monitoring Snooze Basic
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const alertPolicy = new gcp.monitoring.AlertPolicy("alert_policy", {
 *     displayName: "My Alert Policy",
 *     combiner: "OR",
 *     conditions: [{
 *         displayName: "test condition",
 *         conditionThreshold: {
 *             filter: "metric.type=\"compute.googleapis.com/instance/disk/write_bytes_count\" AND resource.type=\"gce_instance\"",
 *             duration: "60s",
 *             comparison: "COMPARISON_GT",
 *             aggregations: [{
 *                 alignmentPeriod: "60s",
 *                 perSeriesAligner: "ALIGN_RATE",
 *             }],
 *         },
 *     }],
 * });
 * const snooze = new gcp.monitoring.Snooze("snooze", {
 *     displayName: "My Snooze",
 *     interval: {
 *         startTime: "2024-02-03T07:44:12Z",
 *         endTime: "2024-02-05T07:44:12Z",
 *     },
 *     criteria: {
 *         policies: [alertPolicy.id],
 *     },
 * });
 * ```
 * ### Monitoring Snooze Filter
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const alertPolicy = new gcp.monitoring.AlertPolicy("alert_policy", {
 *     displayName: "My Alert Policy",
 *     combiner: "OR",
 *     conditions: [{
 *         displayName: "test condition",
 *         conditionThreshold: {
 *             filter: "metric.type=\"compute.googleapis.com/instance/disk/write_bytes_count\" AND resource.type=\"gce_instance\"",
 *             duration: "60s",
 *             comparison: "COMPARISON_GT",
 *             aggregations: [{
 *                 alignmentPeriod: "60s",
 *                 perSeriesAligner: "ALIGN_RATE",
 *             }],
 *         },
 *     }],
 * });
 * const snooze = new gcp.monitoring.Snooze("snooze", {
 *     displayName: "My Snooze",
 *     interval: {
 *         startTime: "2024-02-03T07:44:12Z",
 *         endTime: "2024-02-05T07:44:12Z",
 *     },
 *     criteria: {
 *         policies: [alertPolicy.id],
 *         filter: "resource.labels.instance_id=\"1234567890\"",
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Snooze can be imported using any of these accepted formats:
 *
 * * `projects/{{project}}/snoozes/{{name}}`
 * * `{{project}}/{{name}}`
 * * `{{name}}`
 *
 * When using the `pulumi import` command, Snooze can be imported using one of the formats above. For example:
 *
 * ```sh
 * $ pulumi import gcp:monitoring/snooze:Snooze default projects/{{project}}/snoozes/{{name}}
 * $ pulumi import gcp:monitoring/snooze:Snooze default {{project}}/{{name}}
 * $ pulumi import gcp:monitoring/snooze:Snooze default {{name}}
 * ```
 */
export class Snooze extends pulumi.CustomResource {
    /**
     * Get an existing Snooze resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SnoozeState, opts?: pulumi.CustomResourceOptions): Snooze {
        return new Snooze(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:monitoring/snooze:Snooze';

    /**
     * Returns true if the given object is an instance of Snooze.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Snooze {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Snooze.__pulumiType;
    }

    /**
     * This defines the criteria for applying the Snooze.
     * > **Note:** After a snooze is created, its criteria can't be modified.
     * Changing this block will force replacement of the resource.
     * Structure is documented below.
     */
    declare public readonly criteria: pulumi.Output<outputs.monitoring.SnoozeCriteria>;
    /**
     * A display name for the Snooze. This can be, at most, 512 unicode characters.
     */
    declare public readonly displayName: pulumi.Output<string>;
    /**
     * The Snooze will be active from interval.start_time through interval.end_time.
     * interval.start_time cannot be in the past. There is a 15 second clock skew to
     * account for the time it takes for a request to reach the API from the UI.
     * > **Note:** You can edit the name and period of an upcoming snooze, and you
     * can edit the name and end time of an active snooze. You can't edit a past
     * (expired) snooze.
     * Structure is documented below.
     */
    declare public readonly interval: pulumi.Output<outputs.monitoring.SnoozeInterval>;
    /**
     * Identifier. The name of the Snooze. The format is:
     * projects/[PROJECT_ID_OR_NUMBER]/snoozes/[SNOOZE_ID]
     * The ID of the Snooze will be generated by the system.
     */
    declare public /*out*/ readonly name: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;

    /**
     * Create a Snooze resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SnoozeArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SnoozeArgs | SnoozeState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SnoozeState | undefined;
            resourceInputs["criteria"] = state?.criteria;
            resourceInputs["displayName"] = state?.displayName;
            resourceInputs["interval"] = state?.interval;
            resourceInputs["name"] = state?.name;
            resourceInputs["project"] = state?.project;
        } else {
            const args = argsOrState as SnoozeArgs | undefined;
            if (args?.criteria === undefined && !opts.urn) {
                throw new Error("Missing required property 'criteria'");
            }
            if (args?.displayName === undefined && !opts.urn) {
                throw new Error("Missing required property 'displayName'");
            }
            if (args?.interval === undefined && !opts.urn) {
                throw new Error("Missing required property 'interval'");
            }
            resourceInputs["criteria"] = args?.criteria;
            resourceInputs["displayName"] = args?.displayName;
            resourceInputs["interval"] = args?.interval;
            resourceInputs["project"] = args?.project;
            resourceInputs["name"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Snooze.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Snooze resources.
 */
export interface SnoozeState {
    /**
     * This defines the criteria for applying the Snooze.
     * > **Note:** After a snooze is created, its criteria can't be modified.
     * Changing this block will force replacement of the resource.
     * Structure is documented below.
     */
    criteria?: pulumi.Input<inputs.monitoring.SnoozeCriteria | undefined>;
    /**
     * A display name for the Snooze. This can be, at most, 512 unicode characters.
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * The Snooze will be active from interval.start_time through interval.end_time.
     * interval.start_time cannot be in the past. There is a 15 second clock skew to
     * account for the time it takes for a request to reach the API from the UI.
     * > **Note:** You can edit the name and period of an upcoming snooze, and you
     * can edit the name and end time of an active snooze. You can't edit a past
     * (expired) snooze.
     * Structure is documented below.
     */
    interval?: pulumi.Input<inputs.monitoring.SnoozeInterval | undefined>;
    /**
     * Identifier. The name of the Snooze. The format is:
     * projects/[PROJECT_ID_OR_NUMBER]/snoozes/[SNOOZE_ID]
     * The ID of the Snooze will be generated by the system.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Snooze resource.
 */
export interface SnoozeArgs {
    /**
     * This defines the criteria for applying the Snooze.
     * > **Note:** After a snooze is created, its criteria can't be modified.
     * Changing this block will force replacement of the resource.
     * Structure is documented below.
     */
    criteria: pulumi.Input<inputs.monitoring.SnoozeCriteria>;
    /**
     * A display name for the Snooze. This can be, at most, 512 unicode characters.
     */
    displayName: pulumi.Input<string>;
    /**
     * The Snooze will be active from interval.start_time through interval.end_time.
     * interval.start_time cannot be in the past. There is a 15 second clock skew to
     * account for the time it takes for a request to reach the API from the UI.
     * > **Note:** You can edit the name and period of an upcoming snooze, and you
     * can edit the name and end time of an active snooze. You can't edit a past
     * (expired) snooze.
     * Structure is documented below.
     */
    interval: pulumi.Input<inputs.monitoring.SnoozeInterval>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
}
