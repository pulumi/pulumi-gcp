// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Three different resources help you manage your IAM policy for Network Connectivity Hub. Each of these resources serves a different use case:
 *
 * * `gcp.networkconnectivity.HubIamPolicy`: Authoritative. Sets the IAM policy for the hub and replaces any existing policy already attached.
 * * `gcp.networkconnectivity.HubIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the hub are preserved.
 * * `gcp.networkconnectivity.HubIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the hub are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.networkconnectivity.HubIamPolicy`: Retrieves the IAM policy for the hub
 *
 * > **Note:** `gcp.networkconnectivity.HubIamPolicy` **cannot** be used in conjunction with `gcp.networkconnectivity.HubIamBinding` and `gcp.networkconnectivity.HubIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.networkconnectivity.HubIamBinding` resources **can be** used in conjunction with `gcp.networkconnectivity.HubIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.networkconnectivity.HubIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/networkconnectivity.hubViewer",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.networkconnectivity.HubIamPolicy("policy", {
 *     project: primary.project,
 *     hub: primary.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.networkconnectivity.HubIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.networkconnectivity.HubIamBinding("binding", {
 *     project: primary.project,
 *     hub: primary.name,
 *     role: "roles/networkconnectivity.hubViewer",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.networkconnectivity.HubIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.networkconnectivity.HubIamMember("member", {
 *     project: primary.project,
 *     hub: primary.name,
 *     role: "roles/networkconnectivity.hubViewer",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## This resource supports User Project Overrides.
 *
 * - 
 *
 * # IAM policy for Network Connectivity Hub
 *
 * Three different resources help you manage your IAM policy for Network Connectivity Hub. Each of these resources serves a different use case:
 *
 * * `gcp.networkconnectivity.HubIamPolicy`: Authoritative. Sets the IAM policy for the hub and replaces any existing policy already attached.
 * * `gcp.networkconnectivity.HubIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the hub are preserved.
 * * `gcp.networkconnectivity.HubIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the hub are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.networkconnectivity.HubIamPolicy`: Retrieves the IAM policy for the hub
 *
 * > **Note:** `gcp.networkconnectivity.HubIamPolicy` **cannot** be used in conjunction with `gcp.networkconnectivity.HubIamBinding` and `gcp.networkconnectivity.HubIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.networkconnectivity.HubIamBinding` resources **can be** used in conjunction with `gcp.networkconnectivity.HubIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.networkconnectivity.HubIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/networkconnectivity.hubViewer",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.networkconnectivity.HubIamPolicy("policy", {
 *     project: primary.project,
 *     hub: primary.name,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.networkconnectivity.HubIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.networkconnectivity.HubIamBinding("binding", {
 *     project: primary.project,
 *     hub: primary.name,
 *     role: "roles/networkconnectivity.hubViewer",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.networkconnectivity.HubIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.networkconnectivity.HubIamMember("member", {
 *     project: primary.project,
 *     hub: primary.name,
 *     role: "roles/networkconnectivity.hubViewer",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## Import
 *
 * For all import syntaxes, the "resource in question" can take any of the following forms:
 *
 * * projects/{{project}}/locations/global/hubs/{{name}}
 * * {{project}}/{{name}}
 * * {{name}}
 *
 * Any variables not passed in the import command will be taken from the provider configuration.
 *
 * Network Connectivity hub IAM resources can be imported using the resource identifiers, role, and member.
 *
 * IAM member imports use space-delimited identifiers: the resource in question, the role, and the member identity, e.g.
 * ```sh
 * $ terraform import google_network_connectivity_hub_iam_member.editor "projects/{{project}}/locations/global/hubs/{{hub}} roles/networkconnectivity.hubViewer user:jane@example.com"
 * ```
 *
 * IAM binding imports use space-delimited identifiers: the resource in question and the role, e.g.
 * ```sh
 * $ terraform import google_network_connectivity_hub_iam_binding.editor "projects/{{project}}/locations/global/hubs/{{hub}} roles/networkconnectivity.hubViewer"
 * ```
 *
 * IAM policy imports use the identifier of the resource in question, e.g.
 * ```sh
 * $ pulumi import gcp:networkconnectivity/hubIamPolicy:HubIamPolicy editor projects/{{project}}/locations/global/hubs/{{hub}}
 * ```
 *
 * > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *  full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 */
export class HubIamPolicy extends pulumi.CustomResource {
    /**
     * Get an existing HubIamPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: HubIamPolicyState, opts?: pulumi.CustomResourceOptions): HubIamPolicy {
        return new HubIamPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:networkconnectivity/hubIamPolicy:HubIamPolicy';

    /**
     * Returns true if the given object is an instance of HubIamPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is HubIamPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === HubIamPolicy.__pulumiType;
    }

    /**
     * (Computed) The etag of the IAM policy.
     */
    declare public /*out*/ readonly etag: pulumi.Output<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly hub: pulumi.Output<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    declare public readonly policyData: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;

    /**
     * Create a HubIamPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: HubIamPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: HubIamPolicyArgs | HubIamPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as HubIamPolicyState | undefined;
            resourceInputs["etag"] = state?.etag;
            resourceInputs["hub"] = state?.hub;
            resourceInputs["policyData"] = state?.policyData;
            resourceInputs["project"] = state?.project;
        } else {
            const args = argsOrState as HubIamPolicyArgs | undefined;
            if (args?.hub === undefined && !opts.urn) {
                throw new Error("Missing required property 'hub'");
            }
            if (args?.policyData === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyData'");
            }
            resourceInputs["hub"] = args?.hub;
            resourceInputs["policyData"] = args?.policyData;
            resourceInputs["project"] = args?.project;
            resourceInputs["etag"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(HubIamPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering HubIamPolicy resources.
 */
export interface HubIamPolicyState {
    /**
     * (Computed) The etag of the IAM policy.
     */
    etag?: pulumi.Input<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    hub?: pulumi.Input<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData?: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a HubIamPolicy resource.
 */
export interface HubIamPolicyArgs {
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    hub: pulumi.Input<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string>;
}
