// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * A document schema is used to define document structure.
 *
 * To get more information about DocumentSchema, see:
 *
 * * [API documentation](https://cloud.google.com/document-warehouse/docs/reference/rest/v1/projects.locations.documentSchemas)
 * * How-to Guides
 *     * [Official Documentation](https://cloud.google.com/document-warehouse/docs/manage-document-schemas)
 *
 * ## Example Usage
 * ### Document Ai Warehouse Document Schema Text
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleText = new gcp.documentaiwarehouse.DocumentSchema("exampleText", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-text",
 *     location: "us",
 *     documentIsFolder: false,
 *     propertyDefinitions: [{
 *         name: "prop3",
 *         displayName: "propdisp3",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         textTypeOptions: {},
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Integer
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleInteger = new gcp.documentaiwarehouse.DocumentSchema("exampleInteger", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-integer",
 *     location: "us",
 *     propertyDefinitions: [{
 *         name: "prop1",
 *         displayName: "propdisp1",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         integerTypeOptions: {},
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Float
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleFloat = new gcp.documentaiwarehouse.DocumentSchema("exampleFloat", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-float",
 *     location: "us",
 *     propertyDefinitions: [{
 *         name: "prop2",
 *         displayName: "propdisp2",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         floatTypeOptions: {},
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Property
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleProperty = new gcp.documentaiwarehouse.DocumentSchema("exampleProperty", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-property",
 *     location: "us",
 *     documentIsFolder: false,
 *     propertyDefinitions: [{
 *         name: "prop8",
 *         displayName: "propdisp8",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         propertyTypeOptions: {
 *             propertyDefinitions: [{
 *                 name: "prop8_nested",
 *                 displayName: "propdisp8_nested",
 *                 isRepeatable: false,
 *                 isFilterable: true,
 *                 isSearchable: true,
 *                 isMetadata: false,
 *                 isRequired: false,
 *                 retrievalImportance: "HIGHEST",
 *                 schemaSources: [{
 *                     name: "dummy_source_nested",
 *                     processorType: "dummy_processor_nested",
 *                 }],
 *                 textTypeOptions: {},
 *             }],
 *         },
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Property Enum
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const examplePropertyEnum = new gcp.documentaiwarehouse.DocumentSchema("examplePropertyEnum", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-property",
 *     location: "us",
 *     documentIsFolder: false,
 *     propertyDefinitions: [{
 *         name: "prop8",
 *         displayName: "propdisp8",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         propertyTypeOptions: {
 *             propertyDefinitions: [{
 *                 name: "prop8_nested",
 *                 displayName: "propdisp8_nested",
 *                 isRepeatable: false,
 *                 isFilterable: true,
 *                 isSearchable: true,
 *                 isMetadata: false,
 *                 isRequired: false,
 *                 retrievalImportance: "HIGHEST",
 *                 schemaSources: [{
 *                     name: "dummy_source_nested",
 *                     processorType: "dummy_processor_nested",
 *                 }],
 *                 enumTypeOptions: {
 *                     possibleValues: [
 *                         "M",
 *                         "F",
 *                         "X",
 *                     ],
 *                     validationCheckDisabled: false,
 *                 },
 *             }],
 *         },
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Enum
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleEnum = new gcp.documentaiwarehouse.DocumentSchema("exampleEnum", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-enum",
 *     location: "us",
 *     propertyDefinitions: [{
 *         name: "prop6",
 *         displayName: "propdisp6",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         enumTypeOptions: {
 *             possibleValues: [
 *                 "M",
 *                 "F",
 *                 "X",
 *             ],
 *             validationCheckDisabled: false,
 *         },
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Map
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleMap = new gcp.documentaiwarehouse.DocumentSchema("exampleMap", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-map",
 *     location: "us",
 *     propertyDefinitions: [{
 *         name: "prop4",
 *         displayName: "propdisp4",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         mapTypeOptions: {},
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Datetime
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleDatetime = new gcp.documentaiwarehouse.DocumentSchema("exampleDatetime", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-date_time",
 *     location: "us",
 *     propertyDefinitions: [{
 *         name: "prop7",
 *         displayName: "propdisp7",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         dateTimeTypeOptions: {},
 *     }],
 * });
 * ```
 * ### Document Ai Warehouse Document Schema Timestamp
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const project = gcp.organizations.getProject({});
 * const exampleTimestamp = new gcp.documentaiwarehouse.DocumentSchema("exampleTimestamp", {
 *     projectNumber: project.then(project => project.number),
 *     displayName: "test-property-timestamp",
 *     location: "us",
 *     propertyDefinitions: [{
 *         name: "prop5",
 *         displayName: "propdisp5",
 *         isRepeatable: false,
 *         isFilterable: true,
 *         isSearchable: true,
 *         isMetadata: false,
 *         isRequired: false,
 *         retrievalImportance: "HIGHEST",
 *         schemaSources: [{
 *             name: "dummy_source",
 *             processorType: "dummy_processor",
 *         }],
 *         timestampTypeOptions: {},
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * DocumentSchema can be imported using any of these accepted formats
 *
 * ```sh
 *  $ pulumi import gcp:documentaiwarehouse/documentSchema:DocumentSchema default projects/{{project_number}}/locations/{{location}}/documentSchemas/{{name}}
 * ```
 *
 * ```sh
 *  $ pulumi import gcp:documentaiwarehouse/documentSchema:DocumentSchema default {{project_number}}/{{location}}/{{name}}
 * ```
 */
export class DocumentSchema extends pulumi.CustomResource {
    /**
     * Get an existing DocumentSchema resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DocumentSchemaState, opts?: pulumi.CustomResourceOptions): DocumentSchema {
        return new DocumentSchema(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:documentaiwarehouse/documentSchema:DocumentSchema';

    /**
     * Returns true if the given object is an instance of DocumentSchema.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DocumentSchema {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DocumentSchema.__pulumiType;
    }

    /**
     * Name of the schema given by the user.
     */
    public readonly displayName!: pulumi.Output<string>;
    /**
     * Tells whether the document is a folder or a typical document.
     */
    public readonly documentIsFolder!: pulumi.Output<boolean | undefined>;
    /**
     * The location of the resource.
     */
    public readonly location!: pulumi.Output<string>;
    /**
     * The name of the metadata property.
     *
     * (Optional)
     * The schema name in the source.
     *
     * (Required)
     * The name of the metadata property.
     *
     * (Optional)
     * The schema name in the source.
     */
    public /*out*/ readonly name!: pulumi.Output<string>;
    /**
     * The unique identifier of the project.
     */
    public readonly projectNumber!: pulumi.Output<string>;
    /**
     * Defines the metadata for a schema property.
     * Structure is documented below.
     */
    public readonly propertyDefinitions!: pulumi.Output<outputs.documentaiwarehouse.DocumentSchemaPropertyDefinition[]>;

    /**
     * Create a DocumentSchema resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DocumentSchemaArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DocumentSchemaArgs | DocumentSchemaState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DocumentSchemaState | undefined;
            resourceInputs["displayName"] = state ? state.displayName : undefined;
            resourceInputs["documentIsFolder"] = state ? state.documentIsFolder : undefined;
            resourceInputs["location"] = state ? state.location : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["projectNumber"] = state ? state.projectNumber : undefined;
            resourceInputs["propertyDefinitions"] = state ? state.propertyDefinitions : undefined;
        } else {
            const args = argsOrState as DocumentSchemaArgs | undefined;
            if ((!args || args.displayName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'displayName'");
            }
            if ((!args || args.location === undefined) && !opts.urn) {
                throw new Error("Missing required property 'location'");
            }
            if ((!args || args.projectNumber === undefined) && !opts.urn) {
                throw new Error("Missing required property 'projectNumber'");
            }
            if ((!args || args.propertyDefinitions === undefined) && !opts.urn) {
                throw new Error("Missing required property 'propertyDefinitions'");
            }
            resourceInputs["displayName"] = args ? args.displayName : undefined;
            resourceInputs["documentIsFolder"] = args ? args.documentIsFolder : undefined;
            resourceInputs["location"] = args ? args.location : undefined;
            resourceInputs["projectNumber"] = args ? args.projectNumber : undefined;
            resourceInputs["propertyDefinitions"] = args ? args.propertyDefinitions : undefined;
            resourceInputs["name"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DocumentSchema.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DocumentSchema resources.
 */
export interface DocumentSchemaState {
    /**
     * Name of the schema given by the user.
     */
    displayName?: pulumi.Input<string>;
    /**
     * Tells whether the document is a folder or a typical document.
     */
    documentIsFolder?: pulumi.Input<boolean>;
    /**
     * The location of the resource.
     */
    location?: pulumi.Input<string>;
    /**
     * The name of the metadata property.
     *
     * (Optional)
     * The schema name in the source.
     *
     * (Required)
     * The name of the metadata property.
     *
     * (Optional)
     * The schema name in the source.
     */
    name?: pulumi.Input<string>;
    /**
     * The unique identifier of the project.
     */
    projectNumber?: pulumi.Input<string>;
    /**
     * Defines the metadata for a schema property.
     * Structure is documented below.
     */
    propertyDefinitions?: pulumi.Input<pulumi.Input<inputs.documentaiwarehouse.DocumentSchemaPropertyDefinition>[]>;
}

/**
 * The set of arguments for constructing a DocumentSchema resource.
 */
export interface DocumentSchemaArgs {
    /**
     * Name of the schema given by the user.
     */
    displayName: pulumi.Input<string>;
    /**
     * Tells whether the document is a folder or a typical document.
     */
    documentIsFolder?: pulumi.Input<boolean>;
    /**
     * The location of the resource.
     */
    location: pulumi.Input<string>;
    /**
     * The unique identifier of the project.
     */
    projectNumber: pulumi.Input<string>;
    /**
     * Defines the metadata for a schema property.
     * Structure is documented below.
     */
    propertyDefinitions: pulumi.Input<pulumi.Input<inputs.documentaiwarehouse.DocumentSchemaPropertyDefinition>[]>;
}
