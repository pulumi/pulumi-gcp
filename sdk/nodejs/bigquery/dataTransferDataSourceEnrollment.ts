// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Enrolls a BigQuery Data Transfer Service data source in a project, making it available
 * for use by `gcp.bigquery.DataTransferConfig`. Some data sources, notably Google Cloud
 * Carbon Footprint exports, must be enrolled before a transfer config can be created against
 * them.
 *
 * Enrollment is project-wide: a data source enrolled in a project is available in every
 * location that offers it.
 *
 * Enrollment is eventually consistent, so a newly created or recreated enrollment may briefly
 * read as absent before it settles.
 *
 * To get more information about DataSourceEnrollment, see:
 *
 * * [API documentation](https://cloud.google.com/bigquery/docs/reference/datatransfer/rest/v1/projects/enrollDataSources)
 * * How-to Guides
 *     * [Export your carbon footprint](https://cloud.google.com/carbon-footprint/docs/export)
 *     * [Working with transfers](https://cloud.google.com/bigquery/docs/working-with-transfers)
 *
 * ## Example Usage
 *
 * ### Bigquerydatatransfer Data Source Enrollment Carbon
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const carbon = new gcp.bigquery.DataTransferDataSourceEnrollment("carbon", {
 *     project: "my-project-name",
 *     dataSourceId: "61cede5a-0000-2440-ad42-883d24f8f7b8",
 *     unenrollLocation: "us",
 * });
 * ```
 *
 * ## Import
 *
 * DataSourceEnrollment can be imported using any of these accepted formats:
 *
 * * `projects/{{project}}/dataSources/{{data_source_id}}`
 * * `{{project}}/{{data_source_id}}`
 * * `{{data_source_id}}`
 *
 * When using the `pulumi import` command, DataSourceEnrollment can be imported using one of the formats above. For example:
 *
 * ```sh
 * $ pulumi import gcp:bigquery/dataTransferDataSourceEnrollment:DataTransferDataSourceEnrollment default projects/{{project}}/dataSources/{{data_source_id}}
 * $ pulumi import gcp:bigquery/dataTransferDataSourceEnrollment:DataTransferDataSourceEnrollment default {{project}}/{{data_source_id}}
 * $ pulumi import gcp:bigquery/dataTransferDataSourceEnrollment:DataTransferDataSourceEnrollment default {{data_source_id}}
 * ```
 */
export class DataTransferDataSourceEnrollment extends pulumi.CustomResource {
    /**
     * Get an existing DataTransferDataSourceEnrollment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DataTransferDataSourceEnrollmentState, opts?: pulumi.CustomResourceOptions): DataTransferDataSourceEnrollment {
        return new DataTransferDataSourceEnrollment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:bigquery/dataTransferDataSourceEnrollment:DataTransferDataSourceEnrollment';

    /**
     * Returns true if the given object is an instance of DataTransferDataSourceEnrollment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DataTransferDataSourceEnrollment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DataTransferDataSourceEnrollment.__pulumiType;
    }

    /**
     * The ID of the data source to enroll. For Google Cloud Carbon Footprint exports this is
     * `61cede5a-0000-2440-ad42-883d24f8f7b8`. Call `projects.dataSources.list` to see the data
     * sources currently enrolled in a project.
     */
    declare public readonly dataSourceId: pulumi.Output<string>;
    /**
     * Whether Terraform will be prevented from destroying the resource. Defaults to DELETE.
     * When a 'terraform destroy' or 'pulumi up' would delete the resource,
     * the command will fail if this field is set to "PREVENT" in Terraform state.
     * When set to "ABANDON", the command will remove the resource from Terraform
     * management without updating or deleting the resource in the API.
     * When set to "DELETE", deleting the resource is allowed.
     */
    declare public readonly deletionPolicy: pulumi.Output<string>;
    /**
     * User friendly name of the enrolled data source, for example
     * `Google Cloud Carbon Footprint Exports`.
     */
    declare public /*out*/ readonly displayName: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;
    /**
     * The location whose `unenrollDataSources` endpoint is called when this resource is destroyed.
     * Enrollment itself is project-wide and unenrolling through any location removes it everywhere;
     * this only exists because the API offers no project-level unenroll method. Override it only if
     * `us` is not routable for the project, for example under a data-residency organization policy.
     */
    declare public readonly unenrollLocation: pulumi.Output<string | undefined>;

    /**
     * Create a DataTransferDataSourceEnrollment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DataTransferDataSourceEnrollmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DataTransferDataSourceEnrollmentArgs | DataTransferDataSourceEnrollmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DataTransferDataSourceEnrollmentState | undefined;
            resourceInputs["dataSourceId"] = state?.dataSourceId;
            resourceInputs["deletionPolicy"] = state?.deletionPolicy;
            resourceInputs["displayName"] = state?.displayName;
            resourceInputs["project"] = state?.project;
            resourceInputs["unenrollLocation"] = state?.unenrollLocation;
        } else {
            const args = argsOrState as DataTransferDataSourceEnrollmentArgs | undefined;
            if (args?.dataSourceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'dataSourceId'");
            }
            resourceInputs["dataSourceId"] = args?.dataSourceId;
            resourceInputs["deletionPolicy"] = args?.deletionPolicy;
            resourceInputs["project"] = args?.project;
            resourceInputs["unenrollLocation"] = args?.unenrollLocation;
            resourceInputs["displayName"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DataTransferDataSourceEnrollment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DataTransferDataSourceEnrollment resources.
 */
export interface DataTransferDataSourceEnrollmentState {
    /**
     * The ID of the data source to enroll. For Google Cloud Carbon Footprint exports this is
     * `61cede5a-0000-2440-ad42-883d24f8f7b8`. Call `projects.dataSources.list` to see the data
     * sources currently enrolled in a project.
     */
    dataSourceId?: pulumi.Input<string | undefined>;
    /**
     * Whether Terraform will be prevented from destroying the resource. Defaults to DELETE.
     * When a 'terraform destroy' or 'pulumi up' would delete the resource,
     * the command will fail if this field is set to "PREVENT" in Terraform state.
     * When set to "ABANDON", the command will remove the resource from Terraform
     * management without updating or deleting the resource in the API.
     * When set to "DELETE", deleting the resource is allowed.
     */
    deletionPolicy?: pulumi.Input<string | undefined>;
    /**
     * User friendly name of the enrolled data source, for example
     * `Google Cloud Carbon Footprint Exports`.
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * The location whose `unenrollDataSources` endpoint is called when this resource is destroyed.
     * Enrollment itself is project-wide and unenrolling through any location removes it everywhere;
     * this only exists because the API offers no project-level unenroll method. Override it only if
     * `us` is not routable for the project, for example under a data-residency organization policy.
     */
    unenrollLocation?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a DataTransferDataSourceEnrollment resource.
 */
export interface DataTransferDataSourceEnrollmentArgs {
    /**
     * The ID of the data source to enroll. For Google Cloud Carbon Footprint exports this is
     * `61cede5a-0000-2440-ad42-883d24f8f7b8`. Call `projects.dataSources.list` to see the data
     * sources currently enrolled in a project.
     */
    dataSourceId: pulumi.Input<string>;
    /**
     * Whether Terraform will be prevented from destroying the resource. Defaults to DELETE.
     * When a 'terraform destroy' or 'pulumi up' would delete the resource,
     * the command will fail if this field is set to "PREVENT" in Terraform state.
     * When set to "ABANDON", the command will remove the resource from Terraform
     * management without updating or deleting the resource in the API.
     * When set to "DELETE", deleting the resource is allowed.
     */
    deletionPolicy?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * The location whose `unenrollDataSources` endpoint is called when this resource is destroyed.
     * Enrollment itself is project-wide and unenrolling through any location removes it everywhere;
     * this only exists because the API offers no project-level unenroll method. Override it only if
     * `us` is not routable for the project, for example under a data-residency organization policy.
     */
    unenrollLocation?: pulumi.Input<string | undefined>;
}
