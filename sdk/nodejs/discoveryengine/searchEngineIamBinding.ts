// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Three different resources help you manage your IAM policy for Discovery Engine SearchEngine. Each of these resources serves a different use case:
 *
 * * `gcp.discoveryengine.SearchEngineIamPolicy`: Authoritative. Sets the IAM policy for the searchengine and replaces any existing policy already attached.
 * * `gcp.discoveryengine.SearchEngineIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the searchengine are preserved.
 * * `gcp.discoveryengine.SearchEngineIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the searchengine are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.discoveryengine.SearchEngineIamPolicy`: Retrieves the IAM policy for the searchengine
 *
 * > **Note:** `gcp.discoveryengine.SearchEngineIamPolicy` **cannot** be used in conjunction with `gcp.discoveryengine.SearchEngineIamBinding` and `gcp.discoveryengine.SearchEngineIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.discoveryengine.SearchEngineIamBinding` resources **can be** used in conjunction with `gcp.discoveryengine.SearchEngineIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.discoveryengine.SearchEngineIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/discoveryengine.agentspaceUser",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.discoveryengine.SearchEngineIamPolicy("policy", {
 *     project: basic.project,
 *     location: basic.location,
 *     collectionId: basic.collectionId,
 *     engineId: basic.engineId,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.discoveryengine.SearchEngineIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.discoveryengine.SearchEngineIamBinding("binding", {
 *     project: basic.project,
 *     location: basic.location,
 *     collectionId: basic.collectionId,
 *     engineId: basic.engineId,
 *     role: "roles/discoveryengine.agentspaceUser",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.discoveryengine.SearchEngineIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.discoveryengine.SearchEngineIamMember("member", {
 *     project: basic.project,
 *     location: basic.location,
 *     collectionId: basic.collectionId,
 *     engineId: basic.engineId,
 *     role: "roles/discoveryengine.agentspaceUser",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## This resource supports User Project Overrides.
 *
 * - 
 *
 * # IAM policy for Discovery Engine SearchEngine
 *
 * Three different resources help you manage your IAM policy for Discovery Engine SearchEngine. Each of these resources serves a different use case:
 *
 * * `gcp.discoveryengine.SearchEngineIamPolicy`: Authoritative. Sets the IAM policy for the searchengine and replaces any existing policy already attached.
 * * `gcp.discoveryengine.SearchEngineIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the searchengine are preserved.
 * * `gcp.discoveryengine.SearchEngineIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the searchengine are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.discoveryengine.SearchEngineIamPolicy`: Retrieves the IAM policy for the searchengine
 *
 * > **Note:** `gcp.discoveryengine.SearchEngineIamPolicy` **cannot** be used in conjunction with `gcp.discoveryengine.SearchEngineIamBinding` and `gcp.discoveryengine.SearchEngineIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.discoveryengine.SearchEngineIamBinding` resources **can be** used in conjunction with `gcp.discoveryengine.SearchEngineIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * ## gcp.discoveryengine.SearchEngineIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/discoveryengine.agentspaceUser",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.discoveryengine.SearchEngineIamPolicy("policy", {
 *     project: basic.project,
 *     location: basic.location,
 *     collectionId: basic.collectionId,
 *     engineId: basic.engineId,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * ## gcp.discoveryengine.SearchEngineIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.discoveryengine.SearchEngineIamBinding("binding", {
 *     project: basic.project,
 *     location: basic.location,
 *     collectionId: basic.collectionId,
 *     engineId: basic.engineId,
 *     role: "roles/discoveryengine.agentspaceUser",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * ## gcp.discoveryengine.SearchEngineIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.discoveryengine.SearchEngineIamMember("member", {
 *     project: basic.project,
 *     location: basic.location,
 *     collectionId: basic.collectionId,
 *     engineId: basic.engineId,
 *     role: "roles/discoveryengine.agentspaceUser",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * ## Import
 *
 * For all import syntaxes, the "resource in question" can take any of the following forms:
 *
 * * projects/{{project}}/locations/{{location}}/collections/{{collection_id}}/engines/{{engine_id}}
 * * {{project}}/{{location}}/{{collection_id}}/{{engine_id}}
 * * {{location}}/{{collection_id}}/{{engine_id}}
 * * {{engine_id}}
 *
 * Any variables not passed in the import command will be taken from the provider configuration.
 *
 * Discovery Engine searchengine IAM resources can be imported using the resource identifiers, role, and member.
 *
 * IAM member imports use space-delimited identifiers: the resource in question, the role, and the member identity, e.g.
 * ```sh
 * $ terraform import google_discovery_engine_search_engine_iam_member.editor "projects/{{project}}/locations/{{location}}/collections/{{collection_id}}/engines/{{engine_id}} roles/discoveryengine.agentspaceUser user:jane@example.com"
 * ```
 *
 * IAM binding imports use space-delimited identifiers: the resource in question and the role, e.g.
 * ```sh
 * $ terraform import google_discovery_engine_search_engine_iam_binding.editor "projects/{{project}}/locations/{{location}}/collections/{{collection_id}}/engines/{{engine_id}} roles/discoveryengine.agentspaceUser"
 * ```
 *
 * IAM policy imports use the identifier of the resource in question, e.g.
 * ```sh
 * $ pulumi import gcp:discoveryengine/searchEngineIamBinding:SearchEngineIamBinding editor projects/{{project}}/locations/{{location}}/collections/{{collection_id}}/engines/{{engine_id}}
 * ```
 *
 * > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *  full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 */
export class SearchEngineIamBinding extends pulumi.CustomResource {
    /**
     * Get an existing SearchEngineIamBinding resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SearchEngineIamBindingState, opts?: pulumi.CustomResourceOptions): SearchEngineIamBinding {
        return new SearchEngineIamBinding(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:discoveryengine/searchEngineIamBinding:SearchEngineIamBinding';

    /**
     * Returns true if the given object is an instance of SearchEngineIamBinding.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SearchEngineIamBinding {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SearchEngineIamBinding.__pulumiType;
    }

    /**
     * The collection ID.
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly collectionId: pulumi.Output<string>;
    declare public readonly condition: pulumi.Output<outputs.discoveryengine.SearchEngineIamBindingCondition | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly engineId: pulumi.Output<string>;
    /**
     * (Computed) The etag of the IAM policy.
     */
    declare public /*out*/ readonly etag: pulumi.Output<string>;
    /**
     * Location.
     * Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no location is provided in the parent identifier and no
     * location is specified, it is taken from the provider configuration.
     */
    declare public readonly location: pulumi.Output<string>;
    /**
     * Identities that will be granted the privilege in `role`.
     * Each entry can have one of the following values:
     * * **allUsers**: A special identifier that represents anyone who is on the internet; with or without a Google account.
     * * **allAuthenticatedUsers**: A special identifier that represents anyone who is authenticated with a Google account or a service account.
     * * **user:{emailid}**: An email address that represents a specific Google account. For example, alice@gmail.com or joe@example.com.
     * * **serviceAccount:{emailid}**: An email address that represents a service account. For example, my-other-app@appspot.gserviceaccount.com.
     * * **group:{emailid}**: An email address that represents a Google group. For example, admins@example.com.
     * * **domain:{domain}**: A G Suite domain (primary, instead of alias) name that represents all the users of that domain. For example, google.com or example.com.
     * * **projectOwner:projectid**: Owners of the given project. For example, "projectOwner:my-example-project"
     * * **projectEditor:projectid**: Editors of the given project. For example, "projectEditor:my-example-project"
     * * **projectViewer:projectid**: Viewers of the given project. For example, "projectViewer:my-example-project"
     * * **Federated identities**: One or more federated identities in a workload or workforce identity pool, workload running on GKE, etc. Refer to the [Principal identifiers documentation](https://cloud.google.com/iam/docs/principal-identifiers#allow) for examples of targets and valid configuration. For example, "principal://iam.googleapis.com/locations/global/workforcePools/example-contractors/subject/joe@example.com"
     */
    declare public readonly members: pulumi.Output<string[]>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;
    /**
     * The role that should be applied. Only one
     * `gcp.discoveryengine.SearchEngineIamBinding` can be used per role. Note that custom roles must be of the format
     * `[projects|organizations]/{parent-name}/roles/{role-name}`.
     */
    declare public readonly role: pulumi.Output<string>;

    /**
     * Create a SearchEngineIamBinding resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SearchEngineIamBindingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SearchEngineIamBindingArgs | SearchEngineIamBindingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SearchEngineIamBindingState | undefined;
            resourceInputs["collectionId"] = state?.collectionId;
            resourceInputs["condition"] = state?.condition;
            resourceInputs["engineId"] = state?.engineId;
            resourceInputs["etag"] = state?.etag;
            resourceInputs["location"] = state?.location;
            resourceInputs["members"] = state?.members;
            resourceInputs["project"] = state?.project;
            resourceInputs["role"] = state?.role;
        } else {
            const args = argsOrState as SearchEngineIamBindingArgs | undefined;
            if (args?.collectionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'collectionId'");
            }
            if (args?.engineId === undefined && !opts.urn) {
                throw new Error("Missing required property 'engineId'");
            }
            if (args?.members === undefined && !opts.urn) {
                throw new Error("Missing required property 'members'");
            }
            if (args?.role === undefined && !opts.urn) {
                throw new Error("Missing required property 'role'");
            }
            resourceInputs["collectionId"] = args?.collectionId;
            resourceInputs["condition"] = args?.condition;
            resourceInputs["engineId"] = args?.engineId;
            resourceInputs["location"] = args?.location;
            resourceInputs["members"] = args?.members;
            resourceInputs["project"] = args?.project;
            resourceInputs["role"] = args?.role;
            resourceInputs["etag"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SearchEngineIamBinding.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SearchEngineIamBinding resources.
 */
export interface SearchEngineIamBindingState {
    /**
     * The collection ID.
     * Used to find the parent resource to bind the IAM policy to
     */
    collectionId?: pulumi.Input<string | undefined>;
    condition?: pulumi.Input<inputs.discoveryengine.SearchEngineIamBindingCondition | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    engineId?: pulumi.Input<string | undefined>;
    /**
     * (Computed) The etag of the IAM policy.
     */
    etag?: pulumi.Input<string | undefined>;
    /**
     * Location.
     * Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no location is provided in the parent identifier and no
     * location is specified, it is taken from the provider configuration.
     */
    location?: pulumi.Input<string | undefined>;
    /**
     * Identities that will be granted the privilege in `role`.
     * Each entry can have one of the following values:
     * * **allUsers**: A special identifier that represents anyone who is on the internet; with or without a Google account.
     * * **allAuthenticatedUsers**: A special identifier that represents anyone who is authenticated with a Google account or a service account.
     * * **user:{emailid}**: An email address that represents a specific Google account. For example, alice@gmail.com or joe@example.com.
     * * **serviceAccount:{emailid}**: An email address that represents a service account. For example, my-other-app@appspot.gserviceaccount.com.
     * * **group:{emailid}**: An email address that represents a Google group. For example, admins@example.com.
     * * **domain:{domain}**: A G Suite domain (primary, instead of alias) name that represents all the users of that domain. For example, google.com or example.com.
     * * **projectOwner:projectid**: Owners of the given project. For example, "projectOwner:my-example-project"
     * * **projectEditor:projectid**: Editors of the given project. For example, "projectEditor:my-example-project"
     * * **projectViewer:projectid**: Viewers of the given project. For example, "projectViewer:my-example-project"
     * * **Federated identities**: One or more federated identities in a workload or workforce identity pool, workload running on GKE, etc. Refer to the [Principal identifiers documentation](https://cloud.google.com/iam/docs/principal-identifiers#allow) for examples of targets and valid configuration. For example, "principal://iam.googleapis.com/locations/global/workforcePools/example-contractors/subject/joe@example.com"
     */
    members?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * The role that should be applied. Only one
     * `gcp.discoveryengine.SearchEngineIamBinding` can be used per role. Note that custom roles must be of the format
     * `[projects|organizations]/{parent-name}/roles/{role-name}`.
     */
    role?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SearchEngineIamBinding resource.
 */
export interface SearchEngineIamBindingArgs {
    /**
     * The collection ID.
     * Used to find the parent resource to bind the IAM policy to
     */
    collectionId: pulumi.Input<string>;
    condition?: pulumi.Input<inputs.discoveryengine.SearchEngineIamBindingCondition | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    engineId: pulumi.Input<string>;
    /**
     * Location.
     * Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no location is provided in the parent identifier and no
     * location is specified, it is taken from the provider configuration.
     */
    location?: pulumi.Input<string | undefined>;
    /**
     * Identities that will be granted the privilege in `role`.
     * Each entry can have one of the following values:
     * * **allUsers**: A special identifier that represents anyone who is on the internet; with or without a Google account.
     * * **allAuthenticatedUsers**: A special identifier that represents anyone who is authenticated with a Google account or a service account.
     * * **user:{emailid}**: An email address that represents a specific Google account. For example, alice@gmail.com or joe@example.com.
     * * **serviceAccount:{emailid}**: An email address that represents a service account. For example, my-other-app@appspot.gserviceaccount.com.
     * * **group:{emailid}**: An email address that represents a Google group. For example, admins@example.com.
     * * **domain:{domain}**: A G Suite domain (primary, instead of alias) name that represents all the users of that domain. For example, google.com or example.com.
     * * **projectOwner:projectid**: Owners of the given project. For example, "projectOwner:my-example-project"
     * * **projectEditor:projectid**: Editors of the given project. For example, "projectEditor:my-example-project"
     * * **projectViewer:projectid**: Viewers of the given project. For example, "projectViewer:my-example-project"
     * * **Federated identities**: One or more federated identities in a workload or workforce identity pool, workload running on GKE, etc. Refer to the [Principal identifiers documentation](https://cloud.google.com/iam/docs/principal-identifiers#allow) for examples of targets and valid configuration. For example, "principal://iam.googleapis.com/locations/global/workforcePools/example-contractors/subject/joe@example.com"
     */
    members: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * The role that should be applied. Only one
     * `gcp.discoveryengine.SearchEngineIamBinding` can be used per role. Note that custom roles must be of the format
     * `[projects|organizations]/{parent-name}/roles/{role-name}`.
     */
    role: pulumi.Input<string>;
}
