// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Three different resources help you manage your IAM policy for Identity-Aware Proxy AgentRegistryMcpServer. Each of these resources serves a different use case:
 *
 * * `gcp.iap.AgentRegistryMcpServerIamPolicy`: Authoritative. Sets the IAM policy for the agentregistrymcpserver and replaces any existing policy already attached.
 * * `gcp.iap.AgentRegistryMcpServerIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the agentregistrymcpserver are preserved.
 * * `gcp.iap.AgentRegistryMcpServerIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the agentregistrymcpserver are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.iap.AgentRegistryMcpServerIamPolicy`: Retrieves the IAM policy for the agentregistrymcpserver
 *
 * > **Note:** `gcp.iap.AgentRegistryMcpServerIamPolicy` **cannot** be used in conjunction with `gcp.iap.AgentRegistryMcpServerIamBinding` and `gcp.iap.AgentRegistryMcpServerIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.iap.AgentRegistryMcpServerIamBinding` resources **can be** used in conjunction with `gcp.iap.AgentRegistryMcpServerIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * > **Note:**  This resource supports IAM Conditions but they have some known limitations which can be found [here](https://cloud.google.com/iam/docs/conditions-overview#limitations). Please review this article if you are having issues with IAM Conditions.
 *
 * ## gcp.iap.AgentRegistryMcpServerIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/iap.egressor",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.iap.AgentRegistryMcpServerIamPolicy("policy", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * With IAM Conditions:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/iap.egressor",
 *         members: ["user:jane@example.com"],
 *         condition: {
 *             title: "expires_after_2019_12_31",
 *             description: "Expiring at midnight of 2019-12-31",
 *             expression: "request.time < timestamp(\"2020-01-01T00:00:00Z\")",
 *         },
 *     }],
 * });
 * const policy = new gcp.iap.AgentRegistryMcpServerIamPolicy("policy", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 * ## gcp.iap.AgentRegistryMcpServerIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.iap.AgentRegistryMcpServerIamBinding("binding", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * With IAM Conditions:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.iap.AgentRegistryMcpServerIamBinding("binding", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     members: ["user:jane@example.com"],
 *     condition: {
 *         title: "expires_after_2019_12_31",
 *         description: "Expiring at midnight of 2019-12-31",
 *         expression: "request.time < timestamp(\"2020-01-01T00:00:00Z\")",
 *     },
 * });
 * ```
 * ## gcp.iap.AgentRegistryMcpServerIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.iap.AgentRegistryMcpServerIamMember("member", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * With IAM Conditions:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.iap.AgentRegistryMcpServerIamMember("member", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     member: "user:jane@example.com",
 *     condition: {
 *         title: "expires_after_2019_12_31",
 *         description: "Expiring at midnight of 2019-12-31",
 *         expression: "request.time < timestamp(\"2020-01-01T00:00:00Z\")",
 *     },
 * });
 * ```
 *
 * ## This resource supports User Project Overrides.
 *
 * - 
 *
 * # IAM policy for Identity-Aware Proxy AgentRegistryMcpServer
 *
 * Three different resources help you manage your IAM policy for Identity-Aware Proxy AgentRegistryMcpServer. Each of these resources serves a different use case:
 *
 * * `gcp.iap.AgentRegistryMcpServerIamPolicy`: Authoritative. Sets the IAM policy for the agentregistrymcpserver and replaces any existing policy already attached.
 * * `gcp.iap.AgentRegistryMcpServerIamBinding`: Authoritative for a given role. Updates the IAM policy to grant a role to a list of members. Other roles within the IAM policy for the agentregistrymcpserver are preserved.
 * * `gcp.iap.AgentRegistryMcpServerIamMember`: Non-authoritative. Updates the IAM policy to grant a role to a new member. Other members for the role for the agentregistrymcpserver are preserved.
 *
 * A data source can be used to retrieve policy data in advent you do not need creation
 *
 * * `gcp.iap.AgentRegistryMcpServerIamPolicy`: Retrieves the IAM policy for the agentregistrymcpserver
 *
 * > **Note:** `gcp.iap.AgentRegistryMcpServerIamPolicy` **cannot** be used in conjunction with `gcp.iap.AgentRegistryMcpServerIamBinding` and `gcp.iap.AgentRegistryMcpServerIamMember` or they will fight over what your policy should be.
 *
 * > **Note:** `gcp.iap.AgentRegistryMcpServerIamBinding` resources **can be** used in conjunction with `gcp.iap.AgentRegistryMcpServerIamMember` resources **only if** they do not grant privilege to the same role.
 *
 * > **Note:**  This resource supports IAM Conditions but they have some known limitations which can be found [here](https://cloud.google.com/iam/docs/conditions-overview#limitations). Please review this article if you are having issues with IAM Conditions.
 *
 * ## gcp.iap.AgentRegistryMcpServerIamPolicy
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/iap.egressor",
 *         members: ["user:jane@example.com"],
 *     }],
 * });
 * const policy = new gcp.iap.AgentRegistryMcpServerIamPolicy("policy", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 *
 * With IAM Conditions:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const admin = gcp.organizations.getIAMPolicy({
 *     bindings: [{
 *         role: "roles/iap.egressor",
 *         members: ["user:jane@example.com"],
 *         condition: {
 *             title: "expires_after_2019_12_31",
 *             description: "Expiring at midnight of 2019-12-31",
 *             expression: "request.time < timestamp(\"2020-01-01T00:00:00Z\")",
 *         },
 *     }],
 * });
 * const policy = new gcp.iap.AgentRegistryMcpServerIamPolicy("policy", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     policyData: admin.then(admin => admin.policyData),
 * });
 * ```
 * ## gcp.iap.AgentRegistryMcpServerIamBinding
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.iap.AgentRegistryMcpServerIamBinding("binding", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     members: ["user:jane@example.com"],
 * });
 * ```
 *
 * With IAM Conditions:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const binding = new gcp.iap.AgentRegistryMcpServerIamBinding("binding", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     members: ["user:jane@example.com"],
 *     condition: {
 *         title: "expires_after_2019_12_31",
 *         description: "Expiring at midnight of 2019-12-31",
 *         expression: "request.time < timestamp(\"2020-01-01T00:00:00Z\")",
 *     },
 * });
 * ```
 * ## gcp.iap.AgentRegistryMcpServerIamMember
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.iap.AgentRegistryMcpServerIamMember("member", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     member: "user:jane@example.com",
 * });
 * ```
 *
 * With IAM Conditions:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gcp from "@pulumi/gcp";
 *
 * const member = new gcp.iap.AgentRegistryMcpServerIamMember("member", {
 *     project: _default.project,
 *     location: _default.location,
 *     mcpServerId: _default.mcpServerId,
 *     role: "roles/iap.egressor",
 *     member: "user:jane@example.com",
 *     condition: {
 *         title: "expires_after_2019_12_31",
 *         description: "Expiring at midnight of 2019-12-31",
 *         expression: "request.time < timestamp(\"2020-01-01T00:00:00Z\")",
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * For all import syntaxes, the "resource in question" can take any of the following forms:
 *
 * * projects/{{project}}/locations/{{location}}/iap_web/agentRegistry/mcpServers/{{mcp_server_id}}
 * * {{project}}/{{location}}/{{mcp_server_id}}
 * * {{location}}/{{mcp_server_id}}
 * * {{mcp_server_id}}
 *
 * Any variables not passed in the import command will be taken from the provider configuration.
 *
 * Identity-Aware Proxy agentregistrymcpserver IAM resources can be imported using the resource identifiers, role, and member.
 *
 * IAM member imports use space-delimited identifiers: the resource in question, the role, and the member identity, e.g.
 * ```sh
 * $ terraform import google_iap_agent_registry_mcp_server_iam_member.editor "projects/{{project}}/locations/{{location}}/iap_web/agentRegistry/mcpServers/{{mcp_server_id}} roles/iap.egressor user:jane@example.com"
 * ```
 *
 * IAM binding imports use space-delimited identifiers: the resource in question and the role, e.g.
 * ```sh
 * $ terraform import google_iap_agent_registry_mcp_server_iam_binding.editor "projects/{{project}}/locations/{{location}}/iap_web/agentRegistry/mcpServers/{{mcp_server_id}} roles/iap.egressor"
 * ```
 *
 * IAM policy imports use the identifier of the resource in question, e.g.
 * ```sh
 * $ pulumi import gcp:iap/agentRegistryMcpServerIamPolicy:AgentRegistryMcpServerIamPolicy editor projects/{{project}}/locations/{{location}}/iap_web/agentRegistry/mcpServers/{{mcp_server_id}}
 * ```
 *
 * > **Custom Roles** If you're importing a IAM resource with a custom role, make sure to use the
 *  full name of the custom role, e.g. `[projects/my-project|organizations/my-org]/roles/my-custom-role`.
 */
export class AgentRegistryMcpServerIamPolicy extends pulumi.CustomResource {
    /**
     * Get an existing AgentRegistryMcpServerIamPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AgentRegistryMcpServerIamPolicyState, opts?: pulumi.CustomResourceOptions): AgentRegistryMcpServerIamPolicy {
        return new AgentRegistryMcpServerIamPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gcp:iap/agentRegistryMcpServerIamPolicy:AgentRegistryMcpServerIamPolicy';

    /**
     * Returns true if the given object is an instance of AgentRegistryMcpServerIamPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AgentRegistryMcpServerIamPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AgentRegistryMcpServerIamPolicy.__pulumiType;
    }

    /**
     * (Computed) The etag of the IAM policy.
     */
    declare public /*out*/ readonly etag: pulumi.Output<string>;
    /**
     * The location of the resource. Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no location is provided in the parent identifier and no
     * location is specified, it is taken from the provider configuration.
     */
    declare public readonly location: pulumi.Output<string>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    declare public readonly mcpServerId: pulumi.Output<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    declare public readonly policyData: pulumi.Output<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    declare public readonly project: pulumi.Output<string>;

    /**
     * Create a AgentRegistryMcpServerIamPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AgentRegistryMcpServerIamPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AgentRegistryMcpServerIamPolicyArgs | AgentRegistryMcpServerIamPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AgentRegistryMcpServerIamPolicyState | undefined;
            resourceInputs["etag"] = state?.etag;
            resourceInputs["location"] = state?.location;
            resourceInputs["mcpServerId"] = state?.mcpServerId;
            resourceInputs["policyData"] = state?.policyData;
            resourceInputs["project"] = state?.project;
        } else {
            const args = argsOrState as AgentRegistryMcpServerIamPolicyArgs | undefined;
            if (args?.mcpServerId === undefined && !opts.urn) {
                throw new Error("Missing required property 'mcpServerId'");
            }
            if (args?.policyData === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyData'");
            }
            resourceInputs["location"] = args?.location;
            resourceInputs["mcpServerId"] = args?.mcpServerId;
            resourceInputs["policyData"] = args?.policyData;
            resourceInputs["project"] = args?.project;
            resourceInputs["etag"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AgentRegistryMcpServerIamPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AgentRegistryMcpServerIamPolicy resources.
 */
export interface AgentRegistryMcpServerIamPolicyState {
    /**
     * (Computed) The etag of the IAM policy.
     */
    etag?: pulumi.Input<string | undefined>;
    /**
     * The location of the resource. Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no location is provided in the parent identifier and no
     * location is specified, it is taken from the provider configuration.
     */
    location?: pulumi.Input<string | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    mcpServerId?: pulumi.Input<string | undefined>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a AgentRegistryMcpServerIamPolicy resource.
 */
export interface AgentRegistryMcpServerIamPolicyArgs {
    /**
     * The location of the resource. Used to find the parent resource to bind the IAM policy to. If not specified,
     * the value will be parsed from the identifier of the parent resource. If no location is provided in the parent identifier and no
     * location is specified, it is taken from the provider configuration.
     */
    location?: pulumi.Input<string | undefined>;
    /**
     * Used to find the parent resource to bind the IAM policy to
     */
    mcpServerId: pulumi.Input<string>;
    /**
     * The policy data generated by
     * a `gcp.organizations.getIAMPolicy` data source.
     */
    policyData: pulumi.Input<string>;
    /**
     * The ID of the project in which the resource belongs.
     * If it is not provided, the project will be parsed from the identifier of the parent resource. If no project is provided in the parent identifier and no project is specified, the provider project is used.
     */
    project?: pulumi.Input<string | undefined>;
}
