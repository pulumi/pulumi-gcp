// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.gcp.compute.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.gcp.compute.inputs.RegionBackendServiceHaPolicyLeaderArgs;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class RegionBackendServiceHaPolicyArgs extends com.pulumi.resources.ResourceArgs {

    public static final RegionBackendServiceHaPolicyArgs Empty = new RegionBackendServiceHaPolicyArgs();

    /**
     * Specifies whether fast IP move is enabled, and if so, the mechanism to achieve it.
     * Supported values are:
     * * `DISABLED`: Fast IP Move is disabled. You can only use the haPolicy.leader API to
     *   update the leader.
     * * `GARP_RA`: Provides a method to very quickly define a new network endpoint as the
     *   leader. This method is faster than updating the leader using the
     *   haPolicy.leader API. Fast IP move works as follows: The VM hosting the
     *   network endpoint that should become the new leader sends either a
     *   Gratuitous ARP (GARP) packet (IPv4) or an ICMPv6 Router Advertisement(RA)
     *   packet (IPv6). Google Cloud immediately but temporarily associates the
     *   forwarding rule IP address with that VM, and both new and in-flight packets
     *   are quickly delivered to that VM.
     *   Possible values are: `DISABLED`, `GARP_RA`.
     * 
     */
    @Import(name="fastIpMove")
    private @Nullable Output<String> fastIpMove;

    /**
     * @return Specifies whether fast IP move is enabled, and if so, the mechanism to achieve it.
     * Supported values are:
     * * `DISABLED`: Fast IP Move is disabled. You can only use the haPolicy.leader API to
     *   update the leader.
     * * `GARP_RA`: Provides a method to very quickly define a new network endpoint as the
     *   leader. This method is faster than updating the leader using the
     *   haPolicy.leader API. Fast IP move works as follows: The VM hosting the
     *   network endpoint that should become the new leader sends either a
     *   Gratuitous ARP (GARP) packet (IPv4) or an ICMPv6 Router Advertisement(RA)
     *   packet (IPv6). Google Cloud immediately but temporarily associates the
     *   forwarding rule IP address with that VM, and both new and in-flight packets
     *   are quickly delivered to that VM.
     *   Possible values are: `DISABLED`, `GARP_RA`.
     * 
     */
    public Optional<Output<String>> fastIpMove() {
        return Optional.ofNullable(this.fastIpMove);
    }

    /**
     * Selects one of the network endpoints attached to the backend NEGs of this service as the
     * active endpoint (the leader) that receives all traffic.
     * Structure is documented below.
     * 
     */
    @Import(name="leader")
    private @Nullable Output<RegionBackendServiceHaPolicyLeaderArgs> leader;

    /**
     * @return Selects one of the network endpoints attached to the backend NEGs of this service as the
     * active endpoint (the leader) that receives all traffic.
     * Structure is documented below.
     * 
     */
    public Optional<Output<RegionBackendServiceHaPolicyLeaderArgs>> leader() {
        return Optional.ofNullable(this.leader);
    }

    private RegionBackendServiceHaPolicyArgs() {}

    private RegionBackendServiceHaPolicyArgs(RegionBackendServiceHaPolicyArgs $) {
        this.fastIpMove = $.fastIpMove;
        this.leader = $.leader;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(RegionBackendServiceHaPolicyArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private RegionBackendServiceHaPolicyArgs $;

        public Builder() {
            $ = new RegionBackendServiceHaPolicyArgs();
        }

        public Builder(RegionBackendServiceHaPolicyArgs defaults) {
            $ = new RegionBackendServiceHaPolicyArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param fastIpMove Specifies whether fast IP move is enabled, and if so, the mechanism to achieve it.
         * Supported values are:
         * * `DISABLED`: Fast IP Move is disabled. You can only use the haPolicy.leader API to
         *   update the leader.
         * * `GARP_RA`: Provides a method to very quickly define a new network endpoint as the
         *   leader. This method is faster than updating the leader using the
         *   haPolicy.leader API. Fast IP move works as follows: The VM hosting the
         *   network endpoint that should become the new leader sends either a
         *   Gratuitous ARP (GARP) packet (IPv4) or an ICMPv6 Router Advertisement(RA)
         *   packet (IPv6). Google Cloud immediately but temporarily associates the
         *   forwarding rule IP address with that VM, and both new and in-flight packets
         *   are quickly delivered to that VM.
         *   Possible values are: `DISABLED`, `GARP_RA`.
         * 
         * @return builder
         * 
         */
        public Builder fastIpMove(@Nullable Output<String> fastIpMove) {
            $.fastIpMove = fastIpMove;
            return this;
        }

        /**
         * @param fastIpMove Specifies whether fast IP move is enabled, and if so, the mechanism to achieve it.
         * Supported values are:
         * * `DISABLED`: Fast IP Move is disabled. You can only use the haPolicy.leader API to
         *   update the leader.
         * * `GARP_RA`: Provides a method to very quickly define a new network endpoint as the
         *   leader. This method is faster than updating the leader using the
         *   haPolicy.leader API. Fast IP move works as follows: The VM hosting the
         *   network endpoint that should become the new leader sends either a
         *   Gratuitous ARP (GARP) packet (IPv4) or an ICMPv6 Router Advertisement(RA)
         *   packet (IPv6). Google Cloud immediately but temporarily associates the
         *   forwarding rule IP address with that VM, and both new and in-flight packets
         *   are quickly delivered to that VM.
         *   Possible values are: `DISABLED`, `GARP_RA`.
         * 
         * @return builder
         * 
         */
        public Builder fastIpMove(String fastIpMove) {
            return fastIpMove(Output.of(fastIpMove));
        }

        /**
         * @param leader Selects one of the network endpoints attached to the backend NEGs of this service as the
         * active endpoint (the leader) that receives all traffic.
         * Structure is documented below.
         * 
         * @return builder
         * 
         */
        public Builder leader(@Nullable Output<RegionBackendServiceHaPolicyLeaderArgs> leader) {
            $.leader = leader;
            return this;
        }

        /**
         * @param leader Selects one of the network endpoints attached to the backend NEGs of this service as the
         * active endpoint (the leader) that receives all traffic.
         * Structure is documented below.
         * 
         * @return builder
         * 
         */
        public Builder leader(RegionBackendServiceHaPolicyLeaderArgs leader) {
            return leader(Output.of(leader));
        }

        public RegionBackendServiceHaPolicyArgs build() {
            return $;
        }
    }

}
