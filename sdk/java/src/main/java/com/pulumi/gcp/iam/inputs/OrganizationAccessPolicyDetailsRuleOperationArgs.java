// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.gcp.iam.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.exceptions.MissingRequiredPropertyException;
import java.lang.String;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class OrganizationAccessPolicyDetailsRuleOperationArgs extends com.pulumi.resources.ResourceArgs {

    public static final OrganizationAccessPolicyDetailsRuleOperationArgs Empty = new OrganizationAccessPolicyDetailsRuleOperationArgs();

    /**
     * Specifies the permissions that this rule excludes from the set of
     * affected permissions given by `permissions`. If a permission appears in
     * `permissions` _and_ in `excludedPermissions` then it will _not_ be
     * subject to the policy effect.
     * The excluded permissions can be specified using the same syntax as
     * `permissions`.
     * 
     */
    @Import(name="excludedPermissions")
    private @Nullable Output<List<String>> excludedPermissions;

    /**
     * @return Specifies the permissions that this rule excludes from the set of
     * affected permissions given by `permissions`. If a permission appears in
     * `permissions` _and_ in `excludedPermissions` then it will _not_ be
     * subject to the policy effect.
     * The excluded permissions can be specified using the same syntax as
     * `permissions`.
     * 
     */
    public Optional<Output<List<String>>> excludedPermissions() {
        return Optional.ofNullable(this.excludedPermissions);
    }

    /**
     * The permissions that are explicitly affected by this rule. Each
     * permission uses the format `{service_fqdn}/{resource}.{verb}`, where
     * `{service_fqdn}` is the fully qualified domain name for the service.
     * Currently supported permissions are as follows:
     * * `eventarc.googleapis.com/messageBuses.publish`.
     * 
     */
    @Import(name="permissions", required=true)
    private Output<List<String>> permissions;

    /**
     * @return The permissions that are explicitly affected by this rule. Each
     * permission uses the format `{service_fqdn}/{resource}.{verb}`, where
     * `{service_fqdn}` is the fully qualified domain name for the service.
     * Currently supported permissions are as follows:
     * * `eventarc.googleapis.com/messageBuses.publish`.
     * 
     */
    public Output<List<String>> permissions() {
        return this.permissions;
    }

    private OrganizationAccessPolicyDetailsRuleOperationArgs() {}

    private OrganizationAccessPolicyDetailsRuleOperationArgs(OrganizationAccessPolicyDetailsRuleOperationArgs $) {
        this.excludedPermissions = $.excludedPermissions;
        this.permissions = $.permissions;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(OrganizationAccessPolicyDetailsRuleOperationArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private OrganizationAccessPolicyDetailsRuleOperationArgs $;

        public Builder() {
            $ = new OrganizationAccessPolicyDetailsRuleOperationArgs();
        }

        public Builder(OrganizationAccessPolicyDetailsRuleOperationArgs defaults) {
            $ = new OrganizationAccessPolicyDetailsRuleOperationArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param excludedPermissions Specifies the permissions that this rule excludes from the set of
         * affected permissions given by `permissions`. If a permission appears in
         * `permissions` _and_ in `excludedPermissions` then it will _not_ be
         * subject to the policy effect.
         * The excluded permissions can be specified using the same syntax as
         * `permissions`.
         * 
         * @return builder
         * 
         */
        public Builder excludedPermissions(@Nullable Output<List<String>> excludedPermissions) {
            $.excludedPermissions = excludedPermissions;
            return this;
        }

        /**
         * @param excludedPermissions Specifies the permissions that this rule excludes from the set of
         * affected permissions given by `permissions`. If a permission appears in
         * `permissions` _and_ in `excludedPermissions` then it will _not_ be
         * subject to the policy effect.
         * The excluded permissions can be specified using the same syntax as
         * `permissions`.
         * 
         * @return builder
         * 
         */
        public Builder excludedPermissions(List<String> excludedPermissions) {
            return excludedPermissions(Output.of(excludedPermissions));
        }

        /**
         * @param excludedPermissions Specifies the permissions that this rule excludes from the set of
         * affected permissions given by `permissions`. If a permission appears in
         * `permissions` _and_ in `excludedPermissions` then it will _not_ be
         * subject to the policy effect.
         * The excluded permissions can be specified using the same syntax as
         * `permissions`.
         * 
         * @return builder
         * 
         */
        public Builder excludedPermissions(String... excludedPermissions) {
            return excludedPermissions(List.of(excludedPermissions));
        }

        /**
         * @param permissions The permissions that are explicitly affected by this rule. Each
         * permission uses the format `{service_fqdn}/{resource}.{verb}`, where
         * `{service_fqdn}` is the fully qualified domain name for the service.
         * Currently supported permissions are as follows:
         * * `eventarc.googleapis.com/messageBuses.publish`.
         * 
         * @return builder
         * 
         */
        public Builder permissions(Output<List<String>> permissions) {
            $.permissions = permissions;
            return this;
        }

        /**
         * @param permissions The permissions that are explicitly affected by this rule. Each
         * permission uses the format `{service_fqdn}/{resource}.{verb}`, where
         * `{service_fqdn}` is the fully qualified domain name for the service.
         * Currently supported permissions are as follows:
         * * `eventarc.googleapis.com/messageBuses.publish`.
         * 
         * @return builder
         * 
         */
        public Builder permissions(List<String> permissions) {
            return permissions(Output.of(permissions));
        }

        /**
         * @param permissions The permissions that are explicitly affected by this rule. Each
         * permission uses the format `{service_fqdn}/{resource}.{verb}`, where
         * `{service_fqdn}` is the fully qualified domain name for the service.
         * Currently supported permissions are as follows:
         * * `eventarc.googleapis.com/messageBuses.publish`.
         * 
         * @return builder
         * 
         */
        public Builder permissions(String... permissions) {
            return permissions(List.of(permissions));
        }

        public OrganizationAccessPolicyDetailsRuleOperationArgs build() {
            if ($.permissions == null) {
                throw new MissingRequiredPropertyException("OrganizationAccessPolicyDetailsRuleOperationArgs", "permissions");
            }
            return $;
        }
    }

}
