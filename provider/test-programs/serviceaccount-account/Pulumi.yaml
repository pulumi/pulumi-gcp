name: test

runtime:
  name: yaml

resources:

  provider:
    type: pulumi:providers:gcp
    options:
      version: 6.67.0

  tested-resource:
    type: gcp:serviceAccount:Account
    properties:
      accountId: sa${pulumi.stack}
      displayName: Test Service Account
      project: ${gcp:project}
    options:
      provider: ${provider}
