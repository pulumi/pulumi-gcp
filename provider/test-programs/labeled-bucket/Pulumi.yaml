name: labeled-bucket
runtime: yaml
description: A minimal Google Cloud Pulumi YAML program
outputs:
  bucketId: ${my-bucket.id}
resources:
  my-bucket:
    properties:
      location: US
      labels:
        app: my-bucket
    type: gcp:storage:Bucket
